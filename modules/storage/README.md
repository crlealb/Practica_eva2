# Storage module

Crea un bucket S3 con versioning y bloqueo de acceso público.

Version: v1.0.0

Entradas:
- `bucket_prefix`, `bucket_name`, `suffix_byte_length`, `tags`

Salidas:
- `bucket_name`

Publicar este directorio como repo `terraform-aws-storage` y referenciar por `git::` desde el repo raíz.
